<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- SAAT DIKLIK -->
    <item android:state_pressed="true">
        <shape>
            <solid android:color="@android:color/transparent"/>
            <corners android:radius="28dp"/>
        </shape>
    </item>

    <!-- NORMAL -->
    <item>
        <shape>
            <solid android:color="#00B3A4"/>
            <corners android:radius="28dp"/>
        </shape>
    </item>

</selector>
